# PostgreSQL
POSTGRES_SERVER=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=wallet_pass_db
POSTGRES_DSN=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_SERVER}:5432/${POSTGRES_DB}

# Redis
REDIS_SERVER=redis
REDIS_PORT=6379

# Backend (FastAPI)
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000","http://localhost"]
SECRET_KEY=changeme
API_V1_STR=/api/v1
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7
ENV=development

# Pass Management
APPLE_PASS_TYPE_IDENTIFIER=pass.com.yourcompany.pass
APPLE_TEAM_IDENTIFIER=XXXXXXXXXX
APPLE_CERTIFICATE_PATH=/app/certs/apple_cert.pem
APPLE_PRIVATE_KEY_PATH=/app/certs/apple_key.pem
APPLE_WWDR_CERTIFICATE_PATH=/app/certs/wwdr_cert.pem

# Frontend (NextJS)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1